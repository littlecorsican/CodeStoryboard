export enum TableType {
  SQL = 'sql',
  NOSQL = 'nosql'
}


export enum ColumnType {
  INTEGER = 'integer',
  VARCHAR = 'varchar',
  BOOLEAN = 'boolean',
  DATE = 'date',
  TIME = 'time',
  DATETIME = 'datetime',
  TIMESTAMP = 'timestamp',
  DECIMAL = 'decimal',
  FLOAT = 'float',
  DOUBLE = 'double',
}

export enum PageType {
  BOARD = 'board',
  DBTEMPLATE = 'dbtemplate'
}
